[project]
name = "labs"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
    "pyfakefs>=6.1.3",
    "pytest-cov>=7.0.0",
    "pytest-mock>=3.15.0",
]
